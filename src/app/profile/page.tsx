import React from "react";
import { Profile } from "@/components/profile";
import { Back } from "@/components/back";

export const Page = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default Page;
